# Mahak Volunteer Portal

پورتال وب **سامانه مدیریت داوطلبان محک** (`@mahak/volunteer-portal`).

توسعه:

```bash
cp .env.example .env.local
npm install
npm run dev
```

`API_INTERNAL_URL` درخواست‌های `/api/*` را به `mahak-volunteer-api` پروکسی می‌کند.

مستندات ریشهٔ مخزن را ببینید.
