# Mahak Volunteer Management Platform — copy to .env and adjust for the live host.
# docker compose --env-file .env up -d --build

APP_ENV=development
POSTGRES_PASSWORD=mahak
JWT_SECRET=change-me-in-production
INTERNAL_API_TOKEN=mahak-internal-demo-token
PUBLIC_BASE_URL=http://localhost:3000
CORS_ORIGINS=*
SEED_DEMO=true
JWT_TTL_HOURS=24
DB_MAX_CONNS=40
API_PORT=8080
WEB_PORT=3000
POSTGRES_PORT=5432
REDIS_PORT=6379
